## ============================================================
##  代理后端 A：daed（eBPF 透明代理，替代 nikki / passwall）
##  由 workflow 追加到 360t7.config 之后
##
##  daed 是 eBPF 方案，比传统的 iptables/nftables 重定向性能更好，
##  但它对内核有硬性要求，下面三项缺一不可，否则 defconfig 会
##  因为依赖无法满足而静默丢弃 daed（编出来却没有 daed，很难排查）。
## ============================================================

## ---------- daed 本体 ----------
## 可视化 LuCI 界面（luci-app-daed，内嵌 2023 面板含流量统计）由 files/ 直接烤入，
## 不走 feed —— 旧版纯配置页 luci-app-daede 已弃用并列入瘦身黑名单。
CONFIG_PACKAGE_daed=y

## ---------- daed 依赖的 eBPF 内核能力 ----------
## 1) 内核 BTF：daed 用 CO-RE 加载 eBPF 程序，必须有 BTF 信息。
##    24.10 源码里没有 vmlinux-btf 这个包（已核实 404），
##    所以只能走「内核自带 BTF」这条路 —— 必须打开 DEBUG_INFO_BTF。
##    不开的话 daed 会退回去依赖 vmlinux-btf，包不存在 → 依赖失败。
CONFIG_KERNEL_DEBUG_INFO_BTF=y
## 2) XDP sockets：daed 的 Package/daed 里写了 +@KERNEL_XDP_SOCKETS，
##    显式打开双保险
CONFIG_KERNEL_XDP_SOCKETS=y
## 3) BTF 的两个前置条件（上版固件 daed crash 的根因，缺一不可）：
##    - DEBUG_INFO_BTF 依赖 DEBUG_INFO，必须显式打开；
##    - 24.10 内核默认 DEBUG_INFO_REDUCED=y，会静默砍掉 BTF
##      （BTF 开了也白开），必须显式关闭。
CONFIG_KERNEL_DEBUG_INFO=y
# CONFIG_KERNEL_DEBUG_INFO_REDUCED is not set
## 4) eBPF 工具链：新版 daed（small feed 2026.09.23 起）Makefile 带
##    PKG_BUILD_DEPENDS:=bpf-headers 且 include bpf.mk，编译期要用
##    LLVM/clang>=12 现场编 eBPF 程序（不再是纯 Go 预编译产物）。
##    但 kenzok8 的 daed DEPENDS 漏写了 +@NEED_BPF_TOOLCHAIN（上游正是靠它
##    触发自建 llvm-bpf 工具链），导致 CONFIG_USE_LLVM_BUILD 无人置位，
##    bpf.mk 里 CLANG 解析成 /invalid/clang → bpf-headers 编译失败
##    （报错实锤：LLVM/clang version too old. Minimum required: 12, found: ）。
##    修复：显式打开 SDK_LLVM_BPF（可见选项，defconfig 不会丢弃），
##    它会 select NEED_BPF_TOOLCHAIN → USE_LLVM_BUILD=y → 构建期自建 llvm-bpf。
##    注意不要开 CONFIG_DEVEL：开了反而会关掉 USE_LLVM_BUILD 的默认值。
CONFIG_SDK_LLVM_BPF=y
CONFIG_BPF_TOOLCHAIN_BUILD_LLVM=y

## ---------- daed 运行时依赖（显式列出，避免被 defconfig 裁掉）----------
CONFIG_PACKAGE_kmod-sched-core=y
CONFIG_PACKAGE_kmod-sched-bpf=y
CONFIG_PACKAGE_kmod-veth=y
CONFIG_PACKAGE_ca-bundle=y
CONFIG_PACKAGE_v2ray-geoip=y
CONFIG_PACKAGE_v2ray-geosite=y
